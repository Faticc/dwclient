--[[
Кто заходит на сервер. Единственный файл, который нужно править руками.

Обновление его не трогает (он помечен `keep` в манифесте), так что `update.lua`
можно запускать сколько угодно -- вписанное здесь переживёт.

  username      ник, как на сервере
  uuid          можно с дефисами, можно без
  access_token  идентификатор сессии; берётся снаружи, сам клиент логин и пароль
                не спрашивает и никуда не отправляет

uuid выводится из ника, а не выдаётся наугад: сервер в офлайн-режиме считает его как
UUID версии 3 от строки "OfflinePlayer:<ник>". Проверено на известном значении -- для
ника Fatic формула даёт ровно тот uuid, что стоит у настоящего клиента. Так что при
смене ника uuid надо пересчитать, а не оставить чужой.
]]
return {
    username = "Buyanov",
    uuid = "e4861d55-588d-36d7-9f8b-777e46d60087",

    -- ВПИШИ СЮДА свежий идентификатор сессии:
    access_token = "",
}
